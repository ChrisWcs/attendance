import React from 'react';

const style = {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
};

export default Row = () => (
    <div style={style}>
    </div>
);