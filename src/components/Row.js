import React from 'react';

const style = {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
};

const Row = () => (
    <div style={style}>
    </div>
);

export default Row;